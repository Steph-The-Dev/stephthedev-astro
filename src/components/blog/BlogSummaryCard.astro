---
import { Image } from "astro:assets";
import type { CollectionEntry } from "astro:content";

export interface Props {
	post: CollectionEntry<"blog">;
}

const { post } = Astro.props;
console.log(post);

const { title, description, pubDate, image } = post.data
---

<a href={`/blog/${post.id}/`}>
    <div class="blog__card">
        <Image src={image} alt={title} width={400} height={200} format="avif"/>
        <h2>{title}</h2>
        <p>{description}</p>
        <p>Read post &rarr;</p>
    </div>
</a>

<!-- <div class="hidden sm:inline-block">
    <p class="mt-2 poppins">tags:</p>
    <div class="flex items-center justify-between">
        <ul class="flex flex-wrap gap-4 mt-2">
            {
                post.data.tags.map((tag) => {
                    return (
                        <li>
                            <a href={`/blog/tags/${tag.toLowerCase()}/`}>
                            </a>
                        </li>
                    );
                })
            }
        </ul>
        {
            post.data.draft && (
                <span class="px-4 py-1 text-sm border-2 border-black rounded-full bg-green card-shadow">
                    Draft
                </span>
            )
        }
    </div>
</div> -->

<style>
	.blog__card {
		padding: 1rem;
		border-radius: 10px;
		box-shadow: rgba(0, 0, 0, 0.25) 0px 25px 50px -12px;
	}

    img {
        border-radius: 10px;
        min-width: 100%;
        display: block;
    }
</style>